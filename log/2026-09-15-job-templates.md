# 2026-09-15 — Job templates

Spark: Return visits and adapter builders still had to invent a title, amount, and success criteria every time they opened the demo.

Changed: Create-job on `/#create` has five fill-only presets (research brief, code review, data extract, content draft, filing summary). Clicking one fills the form. It does not create or fund. `GET /api/templates.json` lists the same fields. Discovery pointers updated in `settlement.json`, OpenAPI, `SETTLEMENT.md`, and `llms.txt`. Still demo / `money: false`. No fake traction.
