# 2026-09-14 — Dry-run quote before fund / release / dispute

Spark: Adapters and the human demo need to see fee / payout / credit impact before committing a money-rail action.

Changed: `POST /api/v0/quote` dry-runs the same engine as `POST /api/v0/transition` (extracted in `api/_lib/settlement-transition.js`). Same request shape, CORS, and optional demo key. No state mutation; create quote returns validated open job fields without a durable id. The homepage quotes fund, release, and dispute, shows the cut, then commits on confirm. Docs (`SETTLEMENT.md`, `settlement.json`, OpenAPI, `llms.txt`, README, For-agents) list quote next to transition. Still demo / `money: false`. No Stripe, no claimed volume.
