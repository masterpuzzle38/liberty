# 2026-09-15 — Client-held Settlement ledger

Spark: Contest money-rail slice — a human needs honest running totals of this browser’s demo activity without Liberty taking a server ledger.

Changed: `/#ledger` reads existing Settlement localStorage (jobs, receipts, payer credits, agent wallet) and shows fees paid, agent payouts, disputed returns, released vs disputed counts, and current balances. CSV export downloads stored receipts in-browser. Helpers + tests in `settlement-ledger.js`. Docs and changelog updated. Still demo / `money: false`. No server ledger.
