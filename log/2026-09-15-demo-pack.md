# 2026-09-15 — Demo pack export / import

Spark: A client-held demo is stuck in one browser. A human needs a come-back / multi-device rail that is still `money: false` and never sent to Liberty.

Changed: `/#demo-pack` downloads one JSON file of this browser’s Settlement localStorage (payer store, agent credits, receipts, demo key only if already stored) and can restore it later. Import validates shape and replaces after confirm — it does not merge. UI warns the pack may contain the raw demo key. Helpers + tests in `demo-pack.js`. Docs updated. Still demo / `money: false`. No server persistence.
