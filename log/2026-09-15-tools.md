# 2026-09-15 — Machine-readable adapter tools list

Spark: Agents still had to scrape the homepage to learn which Settlement actions they can call.

Changed: `GET /api/tools.json` lists callable surfaces (quote, transition actions, simulate, verify) with method, path, purpose, and key body fields, plus read-only discovery URLs. Explicit `mode: demo`, `money: false`, `persistence: false`. Not an MCP server, A2A card, or ChatGPT plugin. Served via rewrite to the existing agent discovery function (`?doc=tools`) so Hobby stays at 12 functions. Pointers from `/.well-known/agent.json`, `llms.txt`, For agents, SETTLEMENT.md, and sitemap. Still demo only. No fake traction.
