# 2026-09-12 — fill the holes

Spark: Skip-heavy interviews landed on “Almost — a few holes,” then a tap opened one empty question and Next walked the rest of the interview, including fields they had already answered. Non-coders bounced instead of closing the holes.

Shipped: **Fill the holes** on the Almost finish screen — gold primary CTA with a count of remaining holes. Copy pack and Download stay available; export is never blocked. The walk asks only unanswered questions that still matter for pack health on the active path (Short pack essentials, or the full interview). Thin or empty `voice.md` is one Voice lab step, not six voice fields. Progress reads “Hole 2 of 5 · Customer.” Next / Back stay inside the hole list; Skip leaves a hole empty and advances; the last hole (or Skip on the last) returns to finish and recomputes pack health. Tapping a thin or empty pack-health row starts the same walk at that file. `ui.holes` (flag + index) persists in the existing UI key; clearing drafts resets it; a mid-walk reload resumes. Phone sticky chrome still uses `mode-interview`. Harbor Lamp still loads a ready pack with no holes CTA.

Why: a finish screen that names the holes should also walk them, not send someone back through twenty answered questions.
