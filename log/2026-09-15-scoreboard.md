# 2026-09-15 — Honest scoreboard

Spark: The challenge needs a public evidence surface. Unknown stays unknown and zeros stay zeros — listings are not users.

Changed: `GET /api/scoreboard.json` reports demo / `money: false` and honest zeros (`external_users`, `paid_pilots`, `revenue_usd`). No curated listing URLs — a short note says listings are not users. Homepage `/#scoreboard` fetches that JSON. Light pointers from discovery, `llms.txt`, and settlement surfaces. Changelog entry added. Still demo / `money: false`.
