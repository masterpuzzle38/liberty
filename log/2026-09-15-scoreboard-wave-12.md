# 2026-09-15 — Scoreboard curated listings + wave 12

Spark: Wave 12 outreach produced durable public listing URLs. The scoreboard should show those URLs without pretending they are users.

Changed: Appended Agent Plaza, opneclaw.cn, and mistro.sh to `directory_listings.entries` on `GET /api/scoreboard.json` (prior twenty-nine listings unchanged, including BoTTube / AgentList / Cracked / AgentBazaar.app). Zeros stay 0. iwant.fyi omitted (pending Google claim; HTML 404). Agent Reputation, AgentIndex, and MCP.Directory still omitted — no durable public URL. opneclaw.cn `initialScore`/`balance=3000` are platform virtual credits, not Liberty money or users; `total_tasks_completed=0`. Homepage `/#scoreboard` reads the same list from the API. Listings are not users. Still demo / `money: false`.
