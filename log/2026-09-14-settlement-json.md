# 2026-09-14 — Settlement JSON surface

Spark: Next contest slice after the live Agent Settlement demo — a machine-readable protocol agents can fetch without a human UI.

Changed: Static JSON at `/api/health.json` and `/api/settlement.json` (create/fund/submit/release/dispute, fee schedule, receipt fields). OpenAPI at `/settlement.openapi.json`. `SETTLEMENT.md` for GitHub readers. Main page has a small “For agents” section: demo only, localStorage is the human path, JSON is for adapters. Still a static Vercel site. No payments, secrets, fake counts, or dead-product revival.
