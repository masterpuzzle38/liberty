# 2026-09-15 — Client-held Settlement activity log

Spark: A builder testing adapters needs a glanceable local trail of demo actions without Liberty taking a server log.

Changed: `/#activity` appends create / fund / submit / release / dispute, simulate, verify, and demo pack import / export / reset in this browser’s localStorage (last 80, newest first). Clear activity is confirm-only and does not wipe wallets, jobs, or receipts. Reset demo leaves the log and records the reset. Helpers + tests in `settlement-activity.js`. Docs and changelog updated. Still demo / `money: false`. Liberty never receives the log.
