# 2026-09-15 — Machine-readable fee schedule

Spark: Adapters still had to parse SETTLEMENT.md or the full protocol document to learn the demo cut.

Changed: `GET /api/fees.json` exposes the current demo fee schedule (release 5% rounded `Math.round(amount * 0.05)`, dispute no release fee, top-up/fund none). Same math as the engine — no change. Pointers from settlement surfaces, `/.well-known/agent.json`, `llms.txt`, and the homepage fee schedule / For agents list. Still demo / `money: false`.
