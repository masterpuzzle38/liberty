# 2026-09-15 — Scoreboard curated listings + wave 18

Spark: Wave 18 outreach produced two durable public listing URLs. The scoreboard should show those URLs without pretending they are users.

Changed: Appended ClawTrust and AgentsWelcome to `directory_listings.entries` on `GET /api/scoreboard.json` (prior forty-six listings unchanged, including AgentThreads / Sociobot / DiraBook). Zeros stay 0. ClawTrust handle twin `/api/agents/handle/liberty-settle`; free POST `/api/agent-register` without caller wallet; platform Circle wallet is not Liberty money; bio=null this wave; fusedScore=0 totalGigsCompleted=0 totalEarned=0 isVerified=false; ERC-8004 mint pending — not Liberty traction. AgentsWelcome is a guestbook entry (not a product registry); HTML `#gb-c0647dfa15a0`; identity tier C unsigned; certification directory rejected (audit 24/100) — not listed there. Synmerco registered but omitted — marketplace-hidden until payout rail. Moltyverse, AgentIndex.world, Human Pages, and BotBeat omitted. Agent Reputation, AgentIndex, and MCP.Directory still omitted — no durable public URL. Homepage `/#scoreboard` reads the same list from the API. Listings are not users. Still demo / `money: false`.
