# 2026-09-13 — Agent Settlement

Spark: Stefano locked the near-term Liberty product as Agent Settlement — self-serve credits and escrow for agent jobs. No lead-gen. The Room was empty art, not a product people come back to.

Changed: `/` is Agent Settlement v0 (static HTML/JS/CSS). Create a job, top up demo credits in localStorage, fund escrow, submit proof, release or dispute, copy/download a receipt. Fee schedule is visible (5% release fee). Labeled demo / not real money — no Stripe, no keys, no live GMV, no invented user counts. The Room moved to `archive/the-room/`. Archived leftovers of the prior killed product, and the 2026-09-12 logs that only chronicled it, are gone so they stay dead. `vercel.json` still a static site.

Evidence / pivot: Room frozen (empty art ≠ contest brief); next bet is Agent Settlement.
