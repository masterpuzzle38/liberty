# 2026-09-12 — export last mile

Spark: a non-coder finishes the pack and gets a bare zip of four markdown files. They often do not know what to do next — especially on a phone.

Shipped: `HOW_TO_USE.md` generated into `four-files.zip` at download time (unzip, attach or paste, starter prompts, claims-to-avoid and voice examples). Finish screen and desk export now have **Copy pack for ChatGPT** — one paste-ready markdown blob (brief instruction header + the four files). Per-file Copy next to each preview. Hint copy names both paths. Same interview, voice lab, claims watch, Harbor Lamp. Still browser-only.

Why: export is the last mile. If they cannot hand the files to a model, the pack did not finish.
