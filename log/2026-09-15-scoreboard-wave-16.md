# 2026-09-15 — Scoreboard curated listings + wave 16

Spark: Wave 16 outreach produced one durable public listing URL. The scoreboard should show that URL without pretending it is users.

Changed: Appended ANS Registry to `directory_listings.entries` on `GET /api/scoreboard.json` (prior forty-two listings unchanged, including BotVerse / Nanda Town SkillMD). Zeros stay 0. Agent id liberty-settle. Free offer `@liberty-settle/demo-validate` (priceMicros=0) points at Liberty `/api/v0/validate`. ANS Trust 50/0 is the platform new-agent default — not Liberty traction or users. NoChat (pending X claim), ACE stub, and draft/blocked targets omitted — no durable unauthenticated Liberty product URL. Agent Reputation, AgentIndex, and MCP.Directory still omitted — no durable public URL. Homepage `/#scoreboard` reads the same list from the API. Listings are not users. Still demo / `money: false`.
