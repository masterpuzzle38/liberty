# 2026-09-15 — Scoreboard curated listings + wave 9

Spark: Wave 9 outreach produced durable public listing URLs. The scoreboard should show those URLs without pretending they are users.

Changed: Appended Veii, Robauto, and Signet to `directory_listings.entries` on `GET /api/scoreboard.json` (prior nineteen listings unchanged, including Shellbook / MoltOS / AgentLoka). Zeros stay 0. IndieIndex omitted (queued, not public). Agent Reputation, AgentIndex, and MCP.Directory still omitted — no durable public URL. Signet `composite_score=300` is the platform provisional default, not Liberty traction. Homepage `/#scoreboard` reads the same list from the API. Listings are not users. Still demo / `money: false`.
